# Vercel Production Environment Variables
# Copy this file to configure your Vercel project environment variables
# DO NOT commit actual production values - configure these in Vercel Dashboard

# ===================================
# Frontend Configuration
# ===================================

# Backend API URL (production)
# Example: https://your-backend-api.com/api or https://api.yourdomain.com
NEXT_PUBLIC_API_URL=https://your-backend-api.com/api

# ===================================
# NextAuth.js Configuration
# ===================================

# Frontend URL (production Vercel deployment)
# Example: https://your-app.vercel.app or https://yourdomain.com
NEXTAUTH_URL=https://your-app.vercel.app

# NextAuth secret key for JWT signing
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-secret-key-here-replace-with-actual-secret

# ===================================
# Database Configuration
# ===================================

# PostgreSQL connection string (Vercel Postgres or external)
# Format: postgresql://user:password@host:5432/database?schema=public
DATABASE_URL=postgresql://user:password@host:5432/meeting_management

# Direct database URL (for Prisma migrations if using Vercel Postgres)
DIRECT_URL=postgresql://user:password@host:5432/meeting_management

# ===================================
# Backend Configuration (Not deployed to Vercel)
# ===================================
# Note: Backend runs separately (Railway/Render/etc.)

# Backend JWT Configuration
JWT_SECRET=your-super-secure-secret-min-32-chars-use-random-generator
JWT_EXPIRES_IN=7d
CORS_ORIGIN=https://your-frontend-domain.vercel.app
PORT=3333

# ===================================
# Optional: Third-party Services
# ===================================

# Email service (if sending notifications)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password

# Analytics (if using)
# NEXT_PUBLIC_ANALYTICS_ID=G-XXXXXXXXXX

# Monitoring (if using Sentry)
# SENTRY_DSN=your-sentry-dsn

# ===================================
# Build Configuration (Nx + Vercel)
# ===================================

# Disable Nx Cloud distributed execution for Vercel
NX_CLOUD_DISTRIBUTED_EXECUTION=false

# Disable Nx daemon for Vercel serverless environment
NX_DAEMON=false

# Application Environment
NODE_ENV=production

# ===================================
# How to Configure in Vercel Dashboard
# ===================================
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable with appropriate scope:
#    - Production: for production deployments
#    - Preview: for preview deployments (PR branches)
#    - Development: for local development (optional)
# 4. Use Vercel CLI for sensitive values:
#    vercel env add NEXTAUTH_SECRET production
